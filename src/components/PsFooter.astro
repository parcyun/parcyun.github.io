---
// 모든 페이지 공용 하단 푸터 — 텍스트 나열형, 페이지 콘텐츠 끝(문서 흐름)에 고정.
// 플로팅(position:fixed) 아님 — body를 flex column으로 만들고 footer에 margin-top:auto를
// 줘서 짧은 페이지에서도 뷰포트 바닥에 붙지만 콘텐츠 위에 뜨거나 겹치지 않는다("sticky footer" 패턴).
// MANDATORY(parcyun-studio 스킬 mandatory-footer 규정): "Designed by parcyun studio" 문구·
// 시그니처 폰트/컬러(#FFB11A)·Instagram 링크는 절대 변경/제거 금지.
import { icon } from '../lib/icons';
---
<footer class="ps-footer">
  <div class="ps-footer-inner">
    <nav class="ps-flinks" aria-label="바로가기">
      <a href="/dashboard.html" class="ps-flink">업무 대시보드</a>
      <a href="https://linktr.ee/parcyun" class="ps-flink" target="_blank" rel="noopener">Linktree</a>
      <a href="https://github.com/parcyun" class="ps-flink" target="_blank" rel="noopener">GitHub</a>
      <a href="https://padlet.com/penlayered/parcyun-studio-19rq7rovocsv24vr" class="ps-flink" target="_blank" rel="noopener">Padlet</a>
      <a href="/admin" class="ps-flink">admin</a>
    </nav>
  </div>
</footer>

<!-- ===== 우측 하단 고정(플로팅) 브랜드 마크 — MANDATORY, 스크롤과 무관하게 항상 노출 ===== -->
<div class="ps-brand-fixed">
  <button type="button" class="ps-brand-coffee" id="ps-coffee"><span class="ico" set:html={icon('coffee', 13)}></span> 커피 사주기</button>
  <span class="ps-brand-text">
    Designed by
    <span class="ps-signature">parcyun studio</span>
    <a href="https://www.instagram.com/parcyun" class="ps-ig" target="_blank" rel="noopener">
      <svg class="ps-ig-icon" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2.2c3.2 0 3.6 0 4.8.1 1.2.1 1.8.2 2.2.4.6.2 1 .5 1.4 1 .5.4.8.8 1 1.4.2.4.3 1 .4 2.2.1 1.2.1 1.6.1 4.8s0 3.6-.1 4.8c-.1 1.2-.2 1.8-.4 2.2-.2.6-.5 1-1 1.4-.4.5-.8.8-1.4 1-.4.2-1 .3-2.2.4-1.2.1-1.6.1-4.8.1s-3.6 0-4.8-.1c-1.2-.1-1.8-.2-2.2-.4-.6-.2-1-.5-1.4-1-.5-.4-.8-.8-1-1.4-.2-.4-.3-1-.4-2.2C2.2 15.6 2.2 15.2 2.2 12s0-3.6.1-4.8c.1-1.2.2-1.8.4-2.2.2-.6.5-1 1-1.4.4-.5.8-.8 1.4-1 .4-.2 1-.3 2.2-.4C8.4 2.2 8.8 2.2 12 2.2zm0 5.5a4.3 4.3 0 100 8.6 4.3 4.3 0 000-8.6zm5.4-.3a1 1 0 11-2 0 1 1 0 012 0zM12 9.5a2.5 2.5 0 110 5 2.5 2.5 0 010-5z"/></svg>
      @parcyun
    </a>
  </span>
</div>

<!-- 커피 후원 QR 팝업 -->
<div class="coffee-modal" id="coffee-modal" hidden>
  <div class="coffee-backdrop" data-coffee-close></div>
  <div class="coffee-card" role="dialog" aria-modal="true" aria-labelledby="coffee-title">
    <button class="coffee-close" id="coffee-close" type="button" aria-label="닫기">×</button>
    <div class="coffee-emoji ico" set:html={icon('coffee', 40)}></div>
    <h3 class="coffee-title" id="coffee-title">개발자에게 커피 한 잔</h3>
    <p class="coffee-sub">QR을 스캔해 후원할 수 있어요. 감사합니다!</p>
    <img class="coffee-qr" src="/images/coffee-qr.png" alt="parcyun studio 후원 QR" onerror="this.style.display='none';this.nextElementSibling.style.display='block'" />
    <div class="coffee-qr-fallback">QR 이미지를 불러올 수 없습니다.</div>
    <div class="coffee-label">parcyun studio</div>
  </div>
</div>

<style>
.ps-footer{position:static;flex-shrink:0;margin-top:auto;width:100%;background:rgba(0,0,0,.82);backdrop-filter:blur(10px);-webkit-backdrop-filter:blur(10px);border-top:1px solid rgba(255,255,255,.08)}
.ps-footer-inner{max-width:1108px;margin:0 auto;padding:16px 24px;display:flex;flex-wrap:wrap;align-items:center;gap:8px 20px}
@media(min-width:768px){.ps-footer-inner{padding:18px 48px}}
.ps-flinks{display:flex;flex-wrap:wrap;align-items:center;gap:0;row-gap:4px}
.ps-flink{font-family:var(--ps-font-en);font-size:11px;font-weight:400;letter-spacing:.3px;color:var(--ps-text-cinematic-secondary);text-decoration:none;background:transparent;border:0;cursor:pointer;padding:0 10px;margin-right:0;border-right:1px solid rgba(255,255,255,.14);transition:color .18s}
.ps-flink:last-of-type{border-right:0}
.ps-flink:first-child{padding-left:0}
.ps-flink:hover{color:#fff}
.ico{display:inline-flex;align-items:center;justify-content:center;line-height:0}
.ico :global(svg){display:block}

/* ===== 우측 하단 고정 브랜드 마크(MANDATORY) — 스크롤 무관 항상 플로팅 ===== */
.ps-brand-fixed{position:fixed;right:20px;bottom:calc(var(--ps-footer-h,0px) + 10px);z-index:9989;display:flex;align-items:center;gap:8px;font-family:var(--ps-font-en);font-weight:300;font-size:11px;color:var(--ps-text-cinematic-secondary);white-space:nowrap;background:rgba(0,0,0,.6);backdrop-filter:blur(8px);-webkit-backdrop-filter:blur(8px);padding:4px 12px;border-radius:100px;border:1px solid rgba(255,255,255,.08)}
.ps-brand-coffee{display:inline-flex;align-items:center;gap:4px;font-family:var(--ps-font-en);font-size:11px;font-weight:400;letter-spacing:.3px;color:var(--ps-primary);background:transparent;border:0;padding:0 10px 0 0;margin-right:0;border-right:1px solid rgba(255,255,255,.14);cursor:pointer;transition:color .18s}
.ps-brand-coffee:hover{color:var(--ps-primary-dark)}
.ps-brand-text{display:inline-flex;align-items:center;gap:8px}
.ps-signature{font-family:var(--ps-font-signature);font-size:15px;color:var(--ps-primary);line-height:1}
.ps-ig{display:inline-flex;align-items:center;gap:3px;color:inherit;text-decoration:none}
.ps-ig:hover{color:var(--ps-primary)}
.ps-ig-icon{width:10px;height:10px;vertical-align:middle}
@media(max-width:480px){.ps-brand-fixed{right:14px;font-size:10px;padding:3px 10px;gap:6px}}

.coffee-modal{position:fixed;inset:0;z-index:10000;display:flex;align-items:center;justify-content:center;padding:24px}
.coffee-modal[hidden]{display:none}
.coffee-backdrop{position:absolute;inset:0;background:rgba(0,0,0,.72);backdrop-filter:blur(6px);-webkit-backdrop-filter:blur(6px);animation:coffeeFade .2s var(--ps-ease-out)}
@keyframes coffeeFade{from{opacity:0}to{opacity:1}}
.coffee-card{position:relative;z-index:1;background:var(--ps-surface-cinematic-1);border:1px solid var(--ps-primary);border-radius:18px;padding:32px 32px 26px;text-align:center;max-width:340px;width:100%;box-shadow:0 24px 60px rgba(0,0,0,.6),0 0 40px rgba(255,177,26,.12);animation:coffeePop .28s var(--ps-ease-out)}
@keyframes coffeePop{from{opacity:0;transform:translateY(16px) scale(.96)}to{opacity:1;transform:none}}
.coffee-close{position:absolute;top:12px;right:16px;background:transparent;border:0;color:var(--ps-text-cinematic-secondary);font-size:24px;line-height:1;cursor:pointer;transition:color .15s var(--ps-ease-out)}
.coffee-close:hover{color:var(--ps-primary)}
.coffee-emoji{display:flex;align-items:center;justify-content:center;color:var(--ps-primary);margin-bottom:10px}
.coffee-title{margin:0 0 6px;font-size:19px;font-weight:500;color:#fff}
.coffee-sub{margin:0 0 20px;font-size:13px;font-weight:300;color:var(--ps-text-cinematic-secondary);line-height:1.6}
.coffee-qr{display:block;width:220px;height:220px;margin:0 auto 14px;border-radius:12px;background:#fff;padding:10px;object-fit:contain}
.coffee-qr-fallback{display:none;margin:0 auto 14px;padding:40px 16px;border:1px dashed var(--ps-surface-cinematic-3);border-radius:12px;font-family:var(--ps-font-en);font-size:12px;color:var(--ps-text-cinematic-secondary)}
.coffee-label{font-family:var(--ps-font-signature);font-size:20px;color:var(--ps-primary)}
</style>

<style is:global>
/* sticky footer: body를 flex column으로 — 짧은 페이지도 footer가 뷰포트 바닥에 붙음(플로팅 아님) */
body{display:flex;flex-direction:column;min-height:100vh}
</style>

<script is:inline>
(function(){var m=document.getElementById('coffee-modal');if(!m)return;function o(){m.hidden=false}function c(){m.hidden=true}var b=document.getElementById('ps-coffee');if(b)b.addEventListener('click',o);var x=document.getElementById('coffee-close');if(x)x.addEventListener('click',c);m.addEventListener('click',function(e){if(e.target.hasAttribute('data-coffee-close'))c()});document.addEventListener('keydown',function(e){if(e.key==='Escape'&&!m.hidden)c()});})();
(function(){
  function setVars(){
    var f = document.querySelector('.ps-footer');
    if (f) document.documentElement.style.setProperty('--ps-footer-h', f.getBoundingClientRect().height + 'px');
    var b = document.querySelector('.ps-brand-fixed');
    if (b) {
      var br = b.getBoundingClientRect();
      // 브랜드 pill의 좌표(방문자 카운터 좌측 정렬용)와 높이(위 행 스택 간격용)를 함께 노출
      document.documentElement.style.setProperty('--ps-brand-left', br.left + 'px');
      document.documentElement.style.setProperty('--ps-brand-h', br.height + 'px');
    }
  }
  setVars();
  window.addEventListener('resize', setVars);
  // 시그니처(커스텀 폰트) 로딩 후 pill 너비가 바뀌어 left 좌표가 밀릴 수 있어 재측정
  if (document.fonts && document.fonts.ready) document.fonts.ready.then(setVars);
})();
</script>
<script is:inline src="/ps-track.js" defer></script>
